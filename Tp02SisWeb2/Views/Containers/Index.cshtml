@model IEnumerable<Tp02SisWeb2.Models.Container>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.numContainer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tamanho)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.numContainer)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tipo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.tamanho)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BL.IdBl)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdContainer">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.IdContainer">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdContainer">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
